# OSINT Desk UI

React + Vite frontend. From the repo root, see the main README for run instructions.

```bash
npm install
npm run dev
```

Dev server listens on port 43145 and proxies `/api` to `http://127.0.0.1:8742`.
